import Error404PageConfig from './errors/404/Error404PageConfig';
import ProfileConfig from './profile/ProfileConfig';

const pagesConfigs = [Error404PageConfig, ProfileConfig];

export default pagesConfigs;
